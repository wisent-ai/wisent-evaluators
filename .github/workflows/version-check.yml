name: Version check

# Refuse a change whose declared version disagrees with what it did to the public
# contract. The contract of this package is which evaluators it registers by name, so
# adding one is a capability and removing or renaming one breaks whoever evaluated
# with it yesterday.
#
# The rule itself lives in one place for the whole fleet and is not copied here:
# https://github.com/lbartoszcze/AutoVersion. This workflow supplies only the two
# things this repository alone knows: its surface, and the version it declares.

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  version-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install the shared rule
        run: pip install --quiet "git+https://github.com/lbartoszcze/AutoVersion@v0.1.0"

      - name: Read the public surface
        # No package dependencies: the surface is read from the evaluator classes
        # with ast, so this stays cheap and cannot be broken by an unrelated
        # dependency. A module that does not parse fails the step rather than
        # silently shrinking the surface.
        run: python3 scripts/surface.py > "$RUNNER_TEMP/candidate.json"

      - name: Compare against the released contract
        run: |
          set -euo pipefail
          released="$(jq -r .version released-surface.json)"
          declared="$(awk -F'"' '/version=/{print $2; exit}' setup.py)"
          echo "released: $released"
          echo "declared: $declared"

          verdict="$(autoversion decide \
            --current "$released" \
            --published-surface released-surface.json \
            --candidate-surface "$RUNNER_TEMP/candidate.json" \
            --json)"
          echo "$verdict"

          change="$(printf '%s' "$verdict" | jq -r .change)"
          required="$(printf '%s' "$verdict" | jq -r .next)"

          if [ "$declared" = "$released" ]; then
            if [ "$change" != "internal" ]; then
              echo "::error::The public contract changed ($change) but setup.py still" \
                "declares the released version $released. The next version must be $required."
              false
            fi
            echo "setup.py still declares the released $released, and the contract is unchanged."
          elif [ "$declared" != "$required" ]; then
            echo "::error::setup.py declares $declared, but a $change change to" \
              "$released requires $required."
            false
          else
            echo "Declared version matches the $change change."
          fi

      - name: Check the baseline against the registry
        run: |
          set -euo pipefail
          # released-surface.json must describe what PyPI actually serves, and the
          # check runs both ways. The first token of "source" is a marker naming the
          # tier the baseline came from; scripts/baseline.py is what writes it.
          #
          # A pypi-* baseline that PyPI does not serve was hand-edited, and every
          # later comparison is measured against something nobody can install. A
          # non-registry baseline for a project PyPI *does* serve is worse: it dodges
          # a real release, so the gate would be scored against the working tree.
          marker="$(jq -r '.source | split(" ") | first' released-surface.json)"
          released="$(jq -r .version released-surface.json)"
          echo "marker: $marker"

          case "$marker" in
            pypi-*) claims_registry=yes ;;
            stado:*|git-archive:*|head:*) claims_registry=no ;;
            *) echo "::error::unknown baseline marker: $marker"; false ;;
          esac

          if [ "$claims_registry" = yes ]; then
            if ! curl -sSf "https://pypi.org/pypi/wisent-evaluators/$released/json" >/dev/null; then
              echo "::error::released-surface.json claims $marker, but PyPI does not serve" \
                "wisent-evaluators $released. Run scripts/baseline.py."
              false
            fi
            echo "Baseline $released is served by PyPI."
          else
            if curl -sSf "https://pypi.org/pypi/wisent-evaluators/json" >/dev/null; then
              echo "::error::released-surface.json claims $marker, but PyPI does serve" \
                "wisent-evaluators, so the baseline is dodging a real release." \
                "Run scripts/baseline.py."
              false
            fi
            echo "Nothing published for wisent-evaluators; $marker is the best tier available."
          fi

      - name: Refuse a baseline the registry has moved past
        run: |
          set -euo pipefail
          # Being honest about your tier is not the same as being on the right one.
          # A newer release, or a first sdist, can land after the baseline was
          # generated; the bidirectional check above stays green while the gate
          # quietly measures a superseded artifact. So ask what the best artifact is
          # right now and compare the two things that identify it.
          #
          # Only the version and the tier prefix are read back. The freshly computed
          # SURFACE is deliberately thrown away and the committed baseline is never
          # rewritten here: recomputing both sides at check time is the one shape that
          # structurally cannot refuse, and this must never be "simplified" into it.
          marker="$(jq -r '.source | split(" ") | first' released-surface.json)"
          released="$(jq -r .version released-surface.json)"

          best="$(python3 scripts/baseline.py --stdout)"
          best_version="$(printf '%s' "$best" | jq -r .version)"
          best_marker="$(printf '%s' "$best" | jq -r '.source | split(" ") | first')"

          if [ "${best_marker%%:*}" != "${marker%%:*}" ]; then
            echo "::error::the baseline sits on tier '${marker%%:*}' but '${best_marker%%:*}'" \
              "is reachable now. Run scripts/baseline.py."
            false
          fi
          if [ "$best_version" != "$released" ]; then
            echo "::error::the baseline describes $released, but PyPI now serves" \
              "$best_version, so the gate is measuring a superseded release." \
              "Run scripts/baseline.py."
            false
          fi
          echo "Baseline is on the best reachable tier (${marker%%:*}) at $released."
